media:
  input: src/assets
  output: docs
content:
  - name: posts
    label: Posts
    path: src/posts
    type: collection
    fields:
      - { name: title, label: Title, type: string }
      - { name: date, label: Date, type: date }
      - { name: description, label: Description, type: 
      - { name: author, label: Author, hidden: true, default: Will Rynearson, type: string }
      - { name: body, label: Body, type: rich-text }