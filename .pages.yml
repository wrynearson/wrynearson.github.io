media:
  input: src/assets
  output: docs
content:
  - name: posts
    label: Posts
    path: src/posts
    type: collection
    fields:
      - { name: title, label: Title, type: string }
      - { name: date, label: Date, type: date }
      - { name: description, label: Description, type: string }
      - { name: author, label: Author, hidden: true, default: Will Rynearson, type: string }
      - { name: body, label: Body, type: rich-text }
  - name: posts
    label: Project Pages
    path: src/pages
    type: collection
    fields:
      - { name: title, label: Title, type: string }
      - { name: date, label: Start Date, type: date }
      - { name: end-date, label: End Date, type: date }
      - { name: description, label: Description, type: string }
      - { name: problem, label: Problem, type: string }
      - { name: outcome, label: Outcome, type: string }
      - { name: type, label: Project Type, type: string }
      - { name: type, label: Project Client, type: string }
      - { name: type, label: My Role, type: string }
      - { name: body, label: Body, type: rich-text }